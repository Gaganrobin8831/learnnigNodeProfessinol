<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Form</title>
</head>
<body>
  <form action="/update/<%= users._id %>" method="post">
     <input type="text" value="<%= users.username %>" name="username" required>
     <input type="email" value="<%= users.email %>" name="email" required>
     <input type="password" value="<%= users.password %>" name="password" required>
     <button type="submit">Submit</button>
  </form>
</body>
</html>
